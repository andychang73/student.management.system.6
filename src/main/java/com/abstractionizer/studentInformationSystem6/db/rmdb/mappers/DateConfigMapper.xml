<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.abstractionizer.studentInformationSystem6.db.rmdb.mappers.DateConfigMapper">

    <select id="getDateConfig" resultType="java.lang.String">
        SELECT date
        FROM date_config
        WHERE status = 1
    </select>
</mapper>